```
var var_alejandro:number = 30; //Crear una variable var tipo number con valor 30
console.log(var_alejandro); //Sacar por consola la variable var_alejandro

var_alejandro = 10; //Cambiar el valor de la variable var_alejandro a 10
console.log(var_alejandro); //Sacar por consola la variable var_alejandro

let let_alejandro:String = "Hola món"; //Crear una variable let tipo String con valor "Hola món"
console.log(let_alejandro);//Sacar por consola la variable let_alejandro

let_alejandro = "Adeu món"; //Cambiar el valor de la variable var_alejandro a "Adeu món"
console.log(let_alejandro); //Sacar por consola la variable let_alejandro

const const_alejandro:String = "Javascript obsolet"; //Crear una variable let tipo String con valor "Javascript obsolet"
console.log(const_alejandro);//Sacar por consola la variable const_alejandro

/*
Intento de cambiar el valor de const
const_alejandro = "This dont work"; 


No se puede cambiar el valor de const_alejandro ya que es una
constante y da error:
invalid assignment to const 'const_alejandro'
*/
```